apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: portal
spec:
  hosts:
  - "portal.default.svc.cluster.local"
  http:
  - route:
    - destination:
        host: portal
        port:
          number: 80
