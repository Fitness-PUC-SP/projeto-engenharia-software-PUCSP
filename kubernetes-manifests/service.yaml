apiVersion: v1
kind: Service
metadata:
  labels:
    service-label-key: service-label-value
  name: portal-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
  selector:
    app: portal

    #apiVersion: v1
    #kind: Service
    #metadata:
    #  name: portal
    #spec:
    #  type: ClusterIP
    #  selector:
    #    app: portal
    #  ports:
    #  - name: http
    #    port: 80
    #    targetPort: 8080