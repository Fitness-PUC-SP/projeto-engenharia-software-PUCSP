apiVersion: v1
kind: Service
metadata:
  labels:
    service-label-key: service-label-value
  name: portal-loadbalancer
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      protocol: TCP
  selector:
    deployment-label-key: portal-label

  #apiVersion: v1
  #kind: Service
  #metadata:
  #  name: portal-external
  #spec:
  #  type: LoadBalancer
  #  selector:
  #    app: portal
  #  ports:
  #  - name: http
  #    port: 80
  #    targetPort: 8080