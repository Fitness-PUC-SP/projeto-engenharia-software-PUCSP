apiVersion: v1
kind: Service
metadata:
  labels:
    app: portal
  name: portal-loadbalancer
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      protocol: TCP
  selector:
    app: portal

  #apiVersion: v1
  #kind: Service
  #metadata:
  #  name: portal-external
  #spec:
  #  type: LoadBalancer
  #  selector:
  #    app: portal
  #  ports:
  #  - name: http
  #    port: 80
  #    targetPort: 8080